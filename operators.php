<?php

/*  OPERATORS */

// Arithmetic Operators (+ - * / % **)

// Assignment Operators (= += -= *= /= %= **=)

// String Operators (. •=)

// Comparison Operators (== === != <> !== < > <= >= <=> ?? ?:)

// Error Control Operators (@)
//$x = @file('foo.txt'); // не будет выведена ошибка в случае отсутствия файла - использоваться не рекомендуется

// Increment/Decrement Operators (++ --)

// Logical Operators (&& || ! and or xor)
$x = true;
$y = false;

var_dump($x or $y);

// Bitwise Operators (& | ^ ~ << >>)

// Array Operators (+ == === != !== <>)

// Execution Operators  (``)

// Type Operators (instanceof)

// Nullsafe Operator - PHP8 (?)
